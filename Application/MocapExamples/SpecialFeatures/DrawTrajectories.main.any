// Example drawing a trajectory in the model view based on some 3d variable 
// and a study. The TrajectoryTracer template makes it easy to create the
// drawing. The templates creates a AnyChart object in the model view, but 
// hides the axes. 

#include "../Plug-in-gait_Simple/FullBody.main.any"

Main = 
{
  // Here we are drawing CoM which is data in the global reference frame. 
  // So we must insert our 'TrajectoryTracer' template in a global reference
  // frame.
  EnvironmentModel.GlobalRef = {
     
    #include "<ANYBODY_PATH_MODELUTILS>/Draw/TrajectoryTracer.any"
    TrajectoryTracer CoM_trajectory = {
        Value = &Main.HumanModel.BodyModel.Interface.CenterOfMass.Pos;
        Study = &Main.Studies.MarkerTracking;
        RGB = {0, 1, 1};
        LineWidth = 2;
     };
     
  };
   
  
  
};

