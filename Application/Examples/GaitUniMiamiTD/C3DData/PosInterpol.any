



// filter the data
AnyMatrix PosFiltered = {...LowPassFilterMarker(Pos'[0]), ...LowPassFilterMarker(Pos'[1]), ...LowPassFilterMarker(Pos'[2])};


AnyFunInterpol PosInterpol = 
{
  Type=Bspline;
  BsplineOrder = 4;
  T = ....Header.Frames/....Header.VideoFrameRate;
  Data = .PosFiltered;
};

