AnyFolder Environment = {
  //^ Environment files are used to include objects surrounding human,
  //^ e.g. global reference frame

  AnyFixedRefFrame GlobalRef =
  {
    AnyDrawRefFrame drw={ScaleXYZ=0.3*{1,1,1};};

    AnyRefNode RightFootPrint_H1 = {
      //^ Positioning of the right foot.
      //^ Ground-foot constraints can be excluded with: #define EXCLUDE_FOOT_CONSTRAINTS
      sRel = HeelPosition;
      ARel = RotMat({0,0,0}, ToeDirection, {0,10,0});
      AnyDrawRefFrame drw = {RGB = {0,0.7,0}; ScaleXYZ = 0.1*{1,1,1};};
    };

    AnyRefNode LeftFootPrint_H1 = {
      //^ Positioning of the right foot.
      //^ Ground-foot constraints can be excluded with: #define EXCLUDE_FOOT_CONSTRAINTS
      sRel = HeelPosition;
      ARel = RotMat({0,0,0}, ToeDirection, {0,10,0});
      AnyDrawRefFrame drw = {RGB = {0,0.7,0}; ScaleXYZ = 0.1*{1,1,1};};
    };
//
//    AnyRefNode RightFootPrint_H2 = {
//      //^ Positioning of the right foot.
//      //^ Ground-foot constraints can be excluded with: #define EXCLUDE_FOOT_CONSTRAINTS
//      sRel = HeelPosition;
//      ARel = RotMat({0,0,0}, ToeDirection, {0,10,0});
//      AnyDrawRefFrame drw = {RGB = {0,0.7,0}; ScaleXYZ = 0.1*{1,1,1};};
//    };
//
//    AnyRefNode LeftFootPrint_H2 = {
//      //^ Positioning of the right foot.
//      //^ Ground-foot constraints can be excluded with: #define EXCLUDE_FOOT_CONSTRAINTS
//      sRel = HeelPosition;
//      ARel = RotMat({0,0,0}, ToeDirection, {0,10,0});
//      AnyDrawRefFrame drw = {RGB = {0,0.7,0}; ScaleXYZ = 0.1*{1,1,1};};
//    };
  };
  AnyFixedRefFrame GlobalRef2 =
  {
    AnyDrawRefFrame drw={ScaleXYZ=0.3*{1,1,1};};

//    AnyRefNode RightFootPrint_H1 = {
//      //^ Positioning of the right foot.
//      //^ Ground-foot constraints can be excluded with: #define EXCLUDE_FOOT_CONSTRAINTS
//      sRel = HeelPosition;
//      ARel = RotMat({0,0,0}, ToeDirection, {0,10,0});
//      AnyDrawRefFrame drw = {RGB = {0,0.7,0}; ScaleXYZ = 0.1*{1,1,1};};
//    };
//
//    AnyRefNode LeftFootPrint_H1 = {
//      //^ Positioning of the right foot.
//      //^ Ground-foot constraints can be excluded with: #define EXCLUDE_FOOT_CONSTRAINTS
//      sRel = HeelPosition;
//      ARel = RotMat({0,0,0}, ToeDirection, {0,10,0});
//      AnyDrawRefFrame drw = {RGB = {0,0.7,0}; ScaleXYZ = 0.1*{1,1,1};};
//    };

    AnyRefNode RightFootPrint_H2 = {
      //^ Positioning of the right foot.
      //^ Ground-foot constraints can be excluded with: #define EXCLUDE_FOOT_CONSTRAINTS
      sRel = HeelPosition;
      ARel = RotMat({0,0,0}, ToeDirection, {0,10,0});
      AnyDrawRefFrame drw = {RGB = {0,0.7,0}; ScaleXYZ = 0.1*{1,1,1};};
    };

    AnyRefNode LeftFootPrint_H2 = {
      //^ Positioning of the right foot.
      //^ Ground-foot constraints can be excluded with: #define EXCLUDE_FOOT_CONSTRAINTS
      sRel = HeelPosition;
      ARel = RotMat({0,0,0}, ToeDirection, {0,10,0});
      AnyDrawRefFrame drw = {RGB = {0,0.7,0}; ScaleXYZ = 0.1*{1,1,1};};
    };
  };

};

// Add node on Human 1
Main.Human1.HumanModel.BodyModel.Trunk.SegmentsThorax.ThoraxSeg = {
  Left = {
    AnyRefNode HandContactNode = {
      sRel = ..Scale({0.1,0.4,-0.12});
      ARel = RotMat(10*pi/180,z);
    };
  };
};
