//# Note. Since these tests include main files from other
//# applications they alter the ANYBODY_PATH_MAINFILEDIR and 
//# ANYBODY_PATH_MAINFILE preprocessor flags. 
//#
//# If these flags are important to the model they must be mocked by the
//# test framework. This can be done by adding the following test directives. 
//# path = {'ANYBODY_PATH_MAINFILEDIR':'../../Application/Examples/CrossTrainer',
//#         'ANYBODY_PATH_MAINFILE':'../../Application/Examples/CrossTrainer/CrossTrainer.main.any' }
//#
#include "../libdef.any"

#include "../../Application/Examples/CrossTrainer/CrossTrainer.main.any"

#define CREATE_IMAGE 1


Main = 
{
  AnyBodyStudy TestStudy =
  {
    AnyFolder& Model = .Model;
    tStart = 0;
    tEnd = 1;
    nStep = 30; // Reduced number of steps for the test
    Gravity = {0, -9.81, 0};
  };
  
  
  AnyOperationSequence TestRunApplication = {
    AnyOperation& cal = Main.RunApplication.CalibrationAnal;
    AnyOperation& study = Main.TestStudy.InverseDynamics;
  };
  
  
  
  
  
#ifndef CREATE_IMAGE
  AnyOperation& RunTest = Main.TestRunApplication;
#else
  #include "../Cameras.any"
  AnyOperation& CameraInitPos = Main.Study.InitialConditions;
  LoadTimeCam.CameraLookAtPoint = {0,-0.5,0};
  LoadTimeCam.CameraFieldOfView = DesignVar(2);
  LoadTimeCam.CameraDirection  = {0,0,1};
#endif
  
  
  
  
  
  
  
  
};
