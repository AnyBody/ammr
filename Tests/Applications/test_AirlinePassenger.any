#include "../../Application/Examples/AirlinePassenger/AirlinePassenger.main.any"

Main = 
{
  // assert() expect() (warn() notice())

  
  AnyInt var = warn(0, "hello world");
  
  AnyInt test = gtfun(Main.Study.MaxMuscleActivity, 0.1);

  
  #define EXPECT_ISTEP(TEST, STUDY, ISTEP) AnyInt TestOutput = expect( orfun( neqfun(STUDY.iStep,ISTEP),  TEST ) )
  
  
  EXPECT_ISTEP(test, Main.Study, 1);
  
  AnyOperation& RunTest = Main.RunApplication;
};
