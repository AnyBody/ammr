#include "libdef.any"

Main = 
{
     
   DEFAULT_PARAMETERS_FOLDER Settings(
       NPARAM = 3,
       VAR1 = TrialNumber, 
       VAR2 = Rotation, VAR2_TYPE = AnyMat33,
       VAR3 = Name, VAR3_TYPE = AnyString
   ) = {
       AnyVar _default_TrialNumber = 2;
       AnyMat33 _default_Rotation = RotMat(pi/2,y);
       AnyString _default_Name = "Subject2";
   };
   
   
   // Parameters can now be overridden
   Settings.Rotation = RotMat(pi,y);
   Settings.Name = "Subject2_test";
   
   


};
