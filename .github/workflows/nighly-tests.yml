name: Nightly Full AMMR tests

on:
  schedule:
    - cron: 0 3 * * *

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: pytest -n 4 --instafail --runslow

  
